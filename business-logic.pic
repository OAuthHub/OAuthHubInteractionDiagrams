.PS
copy "inc/init.pic";
step();

begin_frame(Consumer,BusinessFrame,"Business Logic");
active(Consumer);
message(Consumer,ServiceProvider,"GET username (consumer_key,access_token,signature)");
active(ServiceProvider);
return_message(ServiceProvider,Consumer,"200 (username)");
inactive(ServiceProvider);
step();
inactive(Consumer);
end_frame(ServiceProvider,BusinessFrame);

complete(User);
complete(Browser);
complete(Consumer);
complete(ServiceProvider);
.PE
